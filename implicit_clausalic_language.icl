:g // prelude
class Add a {
	(+) :: a -> a -> a
}
class Into a b {
	into :: a -> b
}
:

:m // = :p main = :partition main

// <- = stimuli
shift_from_gravitational_center: real + ranged(0, 360) <- read_io_port(0)

